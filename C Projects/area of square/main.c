#include <stdio.h>


int main(){
 	float l, cir;
 	printf("\nEnter The value of length of the square: ");
 	scanf("%f", &l);
 	cir = l * l;
 	printf("\nThe area of square is %.2f", cir);
	return 0;
}
